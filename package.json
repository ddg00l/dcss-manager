{
  "name": "dcss-manager",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && cp dist/index.html game.html",
    "test": "vitest run",
    "test:watch": "vitest",
    "sim": "node tools/sim/worker.mjs"
  },
  "devDependencies": {
    "vite": "^7.0.0",
    "vite-plugin-singlefile": "^2.2.0",
    "vitest": "^3.2.0"
  }
}